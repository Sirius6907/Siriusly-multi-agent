# Contributing Guide

Contributions are welcome! Please submit pull requests or open issues.

## Development

```sh
pnpm install
pnpm dev
```

## Before Submitting

- Run `pnpm -r typecheck` to check types
- Run `pnpm test:run` to run tests
- Run `pnpm build` to verify the build
